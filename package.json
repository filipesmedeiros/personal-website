{
  "license": "MIT",
  "scripts": {
    "dev": "yarn --cwd frontend dev & yarn --cwd backend dev",
    "stop": "kill $(lsof -t -i:3001) & kill $(lsof -t -i:3000)",
    "postinstall": "yarn --cwd frontend & yarn --cwd backend"
  }
}
